# Usado cuando en Netlify tienes "Base directory" = frontend. No pongas base aquí.
[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
